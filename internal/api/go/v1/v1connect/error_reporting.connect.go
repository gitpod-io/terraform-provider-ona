// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gitpod/v1/error_reporting.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/gitpod-io/terraform-provider-ona/internal/api/go/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ErrorsServiceName is the fully-qualified name of the ErrorsService service.
	ErrorsServiceName = "gitpod.v1.ErrorsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ErrorsServiceReportErrorsProcedure is the fully-qualified name of the ErrorsService's
	// ReportErrors RPC.
	ErrorsServiceReportErrorsProcedure = "/gitpod.v1.ErrorsService/ReportErrors"
)

// ErrorsServiceClient is a client for the gitpod.v1.ErrorsService service.
type ErrorsServiceClient interface {
	// ReportErrors allows clients to report batches of errors that will be sent to error reporting systems.
	// The structure is fully compatible with Sentry's event payload format.
	//
	// Use this method to:
	// - Report client-side errors and exceptions
	// - Track application crashes and panics
	// - Send error context and metadata for debugging
	//
	// ### Examples
	//
	//   - Report a JavaScript error with Sentry-compatible structure:
	//     The service accepts events with comprehensive error information
	//     including stack traces, identity context, breadcrumbs, and metadata
	//     that align with Sentry's event payload format.
	ReportErrors(context.Context, *connect.Request[v1.ReportErrorsRequest]) (*connect.Response[v1.ReportErrorsResponse], error)
}

// NewErrorsServiceClient constructs a client for the gitpod.v1.ErrorsService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewErrorsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ErrorsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	errorsServiceMethods := v1.File_gitpod_v1_error_reporting_proto.Services().ByName("ErrorsService").Methods()
	return &errorsServiceClient{
		reportErrors: connect.NewClient[v1.ReportErrorsRequest, v1.ReportErrorsResponse](
			httpClient,
			baseURL+ErrorsServiceReportErrorsProcedure,
			connect.WithSchema(errorsServiceMethods.ByName("ReportErrors")),
			connect.WithClientOptions(opts...),
		),
	}
}

// errorsServiceClient implements ErrorsServiceClient.
type errorsServiceClient struct {
	reportErrors *connect.Client[v1.ReportErrorsRequest, v1.ReportErrorsResponse]
}

// ReportErrors calls gitpod.v1.ErrorsService.ReportErrors.
func (c *errorsServiceClient) ReportErrors(ctx context.Context, req *connect.Request[v1.ReportErrorsRequest]) (*connect.Response[v1.ReportErrorsResponse], error) {
	return c.reportErrors.CallUnary(ctx, req)
}

// ErrorsServiceHandler is an implementation of the gitpod.v1.ErrorsService service.
type ErrorsServiceHandler interface {
	// ReportErrors allows clients to report batches of errors that will be sent to error reporting systems.
	// The structure is fully compatible with Sentry's event payload format.
	//
	// Use this method to:
	// - Report client-side errors and exceptions
	// - Track application crashes and panics
	// - Send error context and metadata for debugging
	//
	// ### Examples
	//
	//   - Report a JavaScript error with Sentry-compatible structure:
	//     The service accepts events with comprehensive error information
	//     including stack traces, identity context, breadcrumbs, and metadata
	//     that align with Sentry's event payload format.
	ReportErrors(context.Context, *connect.Request[v1.ReportErrorsRequest]) (*connect.Response[v1.ReportErrorsResponse], error)
}

// NewErrorsServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewErrorsServiceHandler(svc ErrorsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	errorsServiceMethods := v1.File_gitpod_v1_error_reporting_proto.Services().ByName("ErrorsService").Methods()
	errorsServiceReportErrorsHandler := connect.NewUnaryHandler(
		ErrorsServiceReportErrorsProcedure,
		svc.ReportErrors,
		connect.WithSchema(errorsServiceMethods.ByName("ReportErrors")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gitpod.v1.ErrorsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ErrorsServiceReportErrorsProcedure:
			errorsServiceReportErrorsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedErrorsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedErrorsServiceHandler struct{}

func (UnimplementedErrorsServiceHandler) ReportErrors(context.Context, *connect.Request[v1.ReportErrorsRequest]) (*connect.Response[v1.ReportErrorsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gitpod.v1.ErrorsService.ReportErrors is not implemented"))
}

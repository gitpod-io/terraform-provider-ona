# `ona_group` import-loop output

The first resource block generated by `terraform query` during validation of
PR #61:

```terraform
resource "ona_group" "all_0" {
  provider    = ona
  description = "For testing only"
  name        = "Project managers"
}
```

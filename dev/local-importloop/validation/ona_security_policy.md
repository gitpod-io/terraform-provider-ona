# `ona_security_policy` import-loop output

The first resource block generated by `terraform query` during validation of
PR #53:

```terraform
resource "ona_security_policy" "all_0" {
  provider        = ona
  name            = "Block npx"
  organization_id = "0191e223-1c3c-7607-badf-303c98b52d2f"
  spec {
    executables {
      default_effect = "allow"
      rule {
        effect = "block"
        path   = "npx"
      }
      rule {
        effect = "block"
        path   = "/usr/bin/npx"
      }
      rule {
        effect = "block"
        path   = "/usr/local/bin/npx"
      }
    }
  }
}
```

# `ona_announcement_banner` import-loop output

The resource block generated by `terraform query` during validation of PR #55:

```terraform
resource "ona_announcement_banner" "all_0" {
  provider = ona
  enabled  = false
  message  = "Please provide your availability for PDE offsite by **Friday 6/20**. [Submit Availability](http://www.ona.com)"
}
```
